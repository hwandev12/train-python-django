# Main settings.py we need to add following codes at first

from django.utils.translation import gettext_lazy as _
LANGUAGES = [
    ('uz', _('Uzbek')),
    ('en', _('English')),
]

# then we need to add the languages at next
